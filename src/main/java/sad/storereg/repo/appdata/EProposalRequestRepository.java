package sad.storereg.repo.appdata;

import org.springframework.data.jpa.repository.JpaRepository;

import sad.storereg.models.appdata.EProposalRequest;

public interface EProposalRequestRepository extends JpaRepository<EProposalRequest, Long>{

}
